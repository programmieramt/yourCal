name: Release Debug APK

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v1.0.0-debug)'
        required: true

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build debug APK
        run: ./gradlew assembleDebug

      - name: Rename APK
        run: |
          mv app/build/outputs/apk/debug/app-debug.apk \
             calorietracker-${{ github.event.inputs.tag }}-debug.apk

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag }}
          name: CalorieTracker ${{ github.event.inputs.tag }} (Debug)
          body: |
            CalorieTracker Debug-Build ${{ github.event.inputs.tag }}.

            **Hinweis:** Unsignierter Debug-Build zum Testen/Sideload, kein offizielles Release.
          draft: false
          prerelease: true
          generate_release_notes: true
          files: calorietracker-${{ github.event.inputs.tag }}-debug.apk
